{
  "name": "ebay-sold-items",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "eBay Developer Program bridge for BEDROCK vault comps — Browse (active) + Marketplace Insights (sold, when entitled)",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test test/*.test.js",
    "smoke": "node src/smoke.js"
  },
  "engines": {
    "node": ">=20"
  }
}
