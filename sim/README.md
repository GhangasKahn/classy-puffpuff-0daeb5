# sim/

Design-spec simulations and visual-system experiments.

Drop single-file HTML prototypes here (no build, no storage). The production PWA lives
in `site/app/`. Keep experiments out of the deployable web root.
