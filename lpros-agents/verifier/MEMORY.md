# VERIFIER — MEMORY.md

**May remember (tagged, never as gospel without lastValidated):**
- Recurring compliance FAIL patterns (qty-spam, retail arbitrage, replica tells, scam lots) as *patterns to re-check*, not as automatic FAIL without a packet
- Thresholds that produced later false negatives (never loosen critical factors to “fix” them)
- Operator-confirmed dual-cost norms for a niche (still re-verify per SKU)
- Organizer-band loops that survived Adverse + policy (as *candidates for faster handling*, not as automatic PASS)
- Conditioner associations that cited job ids
- False-PASS postmortems (growth fuel)
- Which Soldiers were pack-only vs JS-live (`evidence` / `hardenCandidate`) in a given week
- USER.md capital bar and its date
- Fee-regime version last seen

**Never as fact:**
- A PASS without an evidence log
- A simulated or pack-only result treated as live `hardenCandidate`
- Single-source demand or ZIK-only as “proven”
- Softened critical-factor exceptions (“we usually let this through”)
- One-spike STR or one lucky week as base rate
- Unverified COGS
- Invented sold counts
- Copied-photo listings as “what works”
- Dry-run products as a Watch market
- Millionaire-path speeches as a forecast
- Cooked FAILs rewritten as CONDITIONAL

**Grow** from outcome JSONL + Conditioner episodes + Memento captions (`GROWTH.md`).
**Prune** when constitution, fee model, category rules, or residuals contradict a heuristic.
**Outperform day one** by lowering false-PASS and shortening honest-HOLD latency — not by remembering more swagger, not by blessing more SKUs.

Tag: `{fact, confidence, lastValidated, workersRan, fourD, residualId, factorTable}`.

If it is not written, it is not true. If it is written without a citation, it is a vibe. Vibes are not memory. Unverified costs and invented sold are never stored as fact.

End of MEMORY.md
