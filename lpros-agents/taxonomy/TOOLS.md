# TAXONOMY — TOOLS.md

**Allowed**
- Official eBay Browse pagination via playground soldier `crawler` in `lpros-command/src/playground/runner.js` (`runCrawler` → `lpros/src/agents/crawl.js`)
- Official Browse `getItem` enrichment on a watch sample (images metadata, specifics, `/itm/`) — instrument the listing-matrix; do not steal photos
- Internal storage of leaf packets (`lpros/data`, playground jobs)
- Warn: long crawls on local Command `:8790`, not Netlify (~26s)
- Watch VM dock (Page/Term/Net/Proof) as proof of live research when a Watch is the contracted path
- Orchestrator escalation channel when IDENTITY escalation criteria are met
- Root law: `AGENTS.md`, `MEUFT.md`, `USER.md`, `GROWTH.md`, `ROSTER.md`

**Forbidden**
- HTML scrape of `ebay.com/sch` search results or Seller Hub
- Inventing sold counts to decorate a leaf
- Unsolicited marathon crawls from heartbeat or boredom
- Treating n as demand; treating categoryId as destiny
- Copying competitor photos
- Replica language, retail arbitrage
- Auto-list / capital / PASS / LIST / AUTO language
- Calling the Economics Engine as if I own net (`needs_engine` only)
- Exploits, payloads, unauthorized access, credential stuffing
- Storing API keys in markdown

**Rules**
- Record pages, delay, env, timestamp, why the crawl stopped
- THIN and SWAMP are labels, not insults to cover with fake n
- Rate-limit Browse. Prefer fewer high-quality pages over a DOS of the API
- Missing App/Cert → `insufficient_inputs`
- Live missions need `EBAY_ENV=production` + App/Cert in gitignored `.env`. This pack never stores keys.
- Dry-run ≠ live crawl. Skipped in dry-run is honest.
- Terapeak / Insights are paste-assistive via Browser playbook; 403 is not an invitation to scrape Hub HTML
- Saturation = density + junk gravity, not sold volume

**When a tool is missing**
Name the gap. Contract Scout / Intel / Browser for work they own. Do not impersonate them. Do not invent their payload. Do not HTML-scrape as a paperclip. Do not hallucinate a tree. Do not call fashionable nonsense a map.

End of TOOLS.md
