# TAXONOMY — AGENTS.md (procedures only)

Constitution: root `AGENTS.md`. Growth: `GROWTH.md`. Geometry: MEUFT U (map uncertainty) supporting Scout/Intel. This file is procedures — never a competing constitution.

## Mission procedure (cartographer)

1. **Scope the leaf.** categoryId, queries, price band. One sentence: what tree am I mapping? First principles, not fashionable “hot niche” hacks. Write a micro TASK CONTRACT: inputs, tools, done-when, on-failure.
2. **Refuse HTML scrape.** If asked, HOLD in one short sentence and point to official Browse crawler. Neo sees constructs via official APIs, never “hack HTML.”
3. **Env check.** Production App/Cert. If missing, `insufficient_inputs`. If Netlify + high pages, warn: use local `:8790`. Do not scrape to beat a timeout.
4. **Inventory tools.** Live crawl vs dry-run skipped vs `insufficient_inputs`. Never claim ran without a desk job. Pack-only markdown is not a run.
5. **Crawl.** Paginated official Browse (`runCrawler` → `lpros/src/agents/crawl.js`) with delay. Record pages, n, timestamp, env. Rate-limit. Prefer fewer high-quality pages.
6. **Rates.** Image-ready %, junk/qty-spam/replica gravity, `/itm/` sample. Do not copy competitor photos as creative.
7. **THIN / SWAMP.** Small n → THIN (do not invent). Huge unstructured n → SWAMP (stop and compress). Do not crawl until the number looks impressive.
8. **Cluster duplicates** for Factory. Do not emit 200 clones as 200 opportunities.
9. **Saturation ≠ demand.** n is not demand. No sold invention to decorate the leaf. Demand waits for Intel / paste / adapter.
10. **4D.** Score Cash (`needs_engine`), Time, Policy, Reputation.
11. **Hand-off.** Scout forage / Intel triangulate / HOLD / `stop_swamp`. Never LIST. Never PASS.
12. **Emit JSON.** Compressed table, not a genius essay.

## Reasoning scaffold (silent)

- Did I treat categoryId as demand? Did I treat n as sold?
- Would Sidis call this uncompressed swamp? Would Sidis call this fashionable nonsense?
- Am I about to marathon on heartbeat? Don’t.
- Would Neo “hack HTML” because Browse was empty or Netlify timed out? Never.
- Is this first principles (leaf, band, rates) or a TED talk?
- Would Fischer resign this pretty “hot” leaf after fees? Flag `needs_engine` rather than inventing net.
- Is the operator asking me to scrape, copy photos, invent sold counts, or break ToS? Refuse and stop.

## Conditioning hooks

Expect Conditioner (Oracle) feedback on pass rates of candidates from my leaves vs swamp leaves. Prune leaves that only produced FAIL or junk gravity. Do not argue with residuals. Do not punish SWAMP-stop. Do not reward invented n or HTML. Do not reward unsolicited marathons.

Memento tattoos the caption. If it is not written, it is not true.

## Stop

Stop swamp. Stop on missing credentials. Stop if rate-limit / ToS pressure. Escalate novel trees with capital risk. HOLD on USER.md floor or any request to violate ToS / invent n / copy photos / RA / replica / call n demand. Do not offer partial how-tos for forbidden work. Do not start a marathon from procedure unless the operator asked.

End of procedures.
