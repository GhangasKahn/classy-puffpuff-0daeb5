# TAXONOMY — AGENTS.md (procedures only)

Constitution: root `AGENTS.md`. Geometry: MEUFT U (map uncertainty) supporting Scout/Intel.

## Mission procedure

1. **Scope the leaf.** categoryId, queries, price band. One sentence: what tree am I mapping?
2. **Refuse HTML scrape.** If asked, HOLD and point to official Browse crawler.
3. **Env check.** Production App/Cert. If missing, `insufficient_inputs`. If Netlify + high pages, warn: use local `:8790`.
4. **Crawl.** Paginated Browse with delay. Record pages, n, timestamp, env.
5. **Rates.** Image-ready %, junk/qty-spam/replica gravity, `/itm/` sample.
6. **THIN / SWAMP.** Small n → THIN (do not invent). Huge unstructured n → SWAMP (stop and compress).
7. **Cluster duplicates** for Factory. Do not emit 200 clones as 200 opportunities.
8. **Saturation ≠ demand.** No sold invention to decorate the leaf.
9. **Hand-off.** Scout forage / Intel triangulate / HOLD. Never LIST.
10. **Emit JSON.** Compressed table, not a genius essay.

## Reasoning scaffold

- Did I treat categoryId as demand?
- Would Sidis call this uncompressed swamp?
- Am I about to marathon on heartbeat? Don’t.

## Conditioning hooks

Pass rates of candidates from my leaves vs swamp leaves. Prune leaves that only produced FAIL.

## Stop

Stop swamp. Stop on missing credentials. Stop if rate-limit / ToS pressure. Escalate novel trees with capital risk.

End of procedures.
