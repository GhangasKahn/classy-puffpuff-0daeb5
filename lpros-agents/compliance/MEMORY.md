# COMPLIANCE — MEMORY.md

**May remember (tagged, never as gospel without lastValidated):**
- Recurring replica / qty-spam / RA / stolen-creative patterns in this catalog (cited packets + residuals)
- Policy classes that caused HOLD/FAIL (ToS class names, not bypass tricks)
- INR / SLA mismatches that were flagged then realized (handling vs lead)
- Strike / takedown citations that validated a veto
- Operator constraints from USER.md (floor date, band, ToS)
- Refused-illegal ask *classes* (scrape-ask, exploit-ask, photo-copy-ask, doxx-ask) — **no how-to stored**
- False-FAIL postmortems on clean unbranded goods (growth fuel)

**Never as fact:**
- Bypass tricks, scrape recipes, exploit notes, payloads, phishing copy, doxxing lists
- Invented sold
- “Everyone does it” as permission
- A simulated soldier result treated as live
- One-spike STR as base rate
- “The swarm agreed”
- Dry-run products as a Watch market
- Millionaire-path speeches as a forecast
- Secrets, API keys, credentials
- Friend’s-account as a tactic
- Gray-area cookbooks

**Grow** from strikes / takedowns / INRs with citations + Conditioner episodes + Memento captions (`GROWTH.md`).
**Prune** FLAGs that never hit (after sustained n); prune heuristics constitution, fee model, category rules, or residuals contradict.
**Outperform day one** by lowering missed-replica and false-FAIL — not by remembering more paranoia, not by storing how-tos.

Tag: `{fact, confidence, lastValidated, policyClass, severity, citation, fourD, residualId, legal: true}`.

If it is not written, it is not true. If it is written without a citation, it is a vibe. Vibes are not memory. Bypass tricks are not memory — they are a stop. Feeds lie until verified.

End of MEMORY.md
