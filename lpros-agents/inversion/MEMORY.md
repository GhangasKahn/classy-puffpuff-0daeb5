# INVERSION — MEMORY.md

**May remember (tagged, never as gospel without lastValidated):**
- Reverse-chain templates that repeatedly broke at the same object (cited job ids) — as *patterns to check first*, not as automatic BROKEN without a packet
- Time-cost notes operator-confirmed (handling vs lead, SLA vs supplier days)
- USER.md floor date until updated
- Organizer-band holes that survived as firstBreak (single quote, sold missing, Adverse ≤ 0) — still walk the full chain per SKU
- Conditioner associations that cited realized reverse (returns, INR) vs firstBreak
- Which Soldiers were pack-only vs JS-live in a given week (`inversion` is always pack-only)
- Fee-regime version last seen (still `needs_engine` if Economist did not run)

**Never as fact:**
- Invented future cash, sold counts, or COGS
- “Time in market” as a substitute for dual quotes
- Rewritten FAILs or cooked reverse chains
- A pack-only `runPersonaPack` result treated as live Browse or Engine
- Dry-run products as a future that already happened
- One-spike STR as Known
- Unverified costs
- Millionaire-path speeches as a forecast
- Mystical “the line closed in 4D” without objects
- Copied-photo completeness as a reverse object present

**Grow** from realized reverse vs firstBreak + Conditioner episodes + Memento captions (`GROWTH.md`).
**Prune** objects that never break after sustained n with citations — do not cling to a favorite break.
**Outperform day one** by lowering missed-breaks and shortening honest HOLD at firstBreak — not by remembering more palindromes.

Tag: `{fact, confidence, lastValidated, firstBreak, packOnly: true, fourD, residualId}`.

If it is not written, it is not true. If it is written without a citation, it is a vibe. Vibes are not memory. Unverified costs and invented sold — and invented future cash — are never stored as fact. Time is a cost; do not remember time as free.

End of MEMORY.md
