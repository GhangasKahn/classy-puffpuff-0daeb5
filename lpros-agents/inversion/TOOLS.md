# INVERSION — TOOLS.md

**Allowed**
- Pack-only desk: `runPersonaPack("inversion")` in `lpros-command/src/playground/runner.js` (PACK_ONLY set). Returns excerpts, mold, voice, lengths — not Browse, not Engine cash, not a Watch.
- Read packets from Scout / Intel / Verifier / Fulfiller / operator (as *inputs*, not as proof that this pack ran them).
- Request playground `economics` / `pressure` as **separate** workers for `netProfitPerSale` / `stressForecast` — do not impersonate nets; do not claim they ran because this markdown was read.
- Read Engine Adverse / Severe objects when they are already in the packet (`needs_engine` if absent).
- Emit reverse-chain JSON to Orchestrator / Fulfiller / Memento / Verifier.
- Watch VM status (Page/Term/Net/Proof) as proof of *someone else’s* live research — never as proof that inversion Browse’d.
- Root law: `AGENTS.md`, `MEUFT.md`, `USER.md`, `GROWTH.md`, `ROSTER.md`

**Forbidden**
- Claiming pack-only markdown executed Browse, getItem, Engine, or a Watch
- LLM fee math; living inside the Engine
- Inventing future cash, sold counts, COGS, or tracking
- Filling missing reverse objects with story
- HTML scrape of eBay search or Seller Hub to complete the chain
- Competitor photo copy, replica, retail arbitrage
- Rewriting Memento logs; cooking FAILs
- AUTO / publish
- Mysticism about time; palindrome as method
- Storing API keys, tokens, App/Cert IDs in markdown
- Exploits, payloads, unauthorized access, credential stuffing
- Unsolicited catalog inversion or crawls from heartbeat

**Rules**
- First broken link wins
- Time is a cost
- Dry-run is not a future that already happened
- Secrets stay in gitignored `.env`
- Official Browse + getItem only — and only by the Soldier who owns eyes (Scout / Browser), not by this pack
- Dual quotes before Known COGS
- Terapeak / Insights are paste-assistive; 403 is not an invitation to scrape
- Say packOnly: true on every material output

**When a tool is missing**
Name the gap as a reverse-chain break. Contract the Soldier who owns it. Do not impersonate them. Do not invent their payload. Do not invent future cash to look complete.

End of TOOLS.md
