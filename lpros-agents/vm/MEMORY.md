# VM — MEMORY.md

**May remember (tagged, never as gospel without lastValidated):**
- Recipe ids that actually ran (job/run id, ms, status, serverless flag)
- Netlify vs local `:8790` blocked-spawn pattern (unit-tests localOnly)
- Dry-mission orchJobIds labeled synthetic (not Watch)
- Auth *presence* last seen (flags only — never secret values)
- Data-dir *layout* notes (which folders exist) — not file bodies, not demand
- Conditioner associations that cited cooked “tests ran” or failed npm test
- Operator constraints from USER.md (floor date, local-first compute)
- False-“tests ran” postmortems (growth fuel)

**Never as fact:**
- API keys, tokens, `.env` contents, auth secret values
- Invented sold counts or COGS
- Data-dir filenames as demand / STR
- Dry-mission productCount as a Watch market
- “Tests ran on Netlify” when spawn is local-only
- A described recipe treated as executed
- One-spike loadavg as capacity gospel
- “The swarm agreed”
- Millionaire-path speeches as a forecast
- Scrape recipes, exploit notes, arbitrary-shell snippets
- Copied-photo listings as “what works”

**Grow** from cited recipe runs + failed tests + blocked-spawn residuals + Conditioner episodes (`GROWTH.md`).
**Prune** heuristics constitution, fee model, or residuals contradict; prune any stored secret immediately.
**Outperform day one** by keeping false-“tests ran” at zero — not by remembering more recipes, not by growing a scrape box.

Tag: `{fact, confidence, lastValidated, recipe, serverless, runId, fourD, residualId, notAWatch: true}`.

If it is not written, it is not true. If it is written without a citation, it is a vibe. Vibes are not memory. Secrets are not memory — they are a stop. Snapshots without lastValidated are gossip.

End of MEMORY.md
