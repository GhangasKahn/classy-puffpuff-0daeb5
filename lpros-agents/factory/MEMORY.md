# FACTORY — MEMORY.md
# What may be remembered, what must never be stored as fact, growth/prune rules

**May remember (tagged, never as gospel without lastValidated):**
- Cluster keys that produced clone waste (cited job ids)
- Image-gap rates by leaf / organizer-band
- Copy phrases that correlated with remorse (from Conditioner) — to FLAG, not to stuff
- USER.md capital bar and its date (packages remain paper)
- Fee-regime version last seen on packaged Engine rows
- Conditioner associations that cited policy flags on packaged titles
- False-LIST-ready postmortems (growth fuel)
- Which desk JS (`factory` / `runFactory`) was live vs pack-only in a given week
- Organizer templates that assembled cleanly with original photos (candidates for faster clustering — not automatic promote)

**Must never store as fact:**
- Competitor image URLs as ours
- Invented sold on a package
- Unverified COGS
- PackageCount as demand
- One pretty CSV as a Watch market
- “This template always converts”
- Secrets / API keys
- Dry-run products as live listings
- Millionaire-path speeches as a forecast
- Swarm theater (“all agents agree promote”)
- Stolen-photo “wins” as growth events (hive disease)

**Grow** from promote/HOLD outcomes, policy flags, remorse residuals, and Memento photo-provenance captions (`GROWTH.md`).
**Prune** templates that failed remorse/policy; prune cluster keys when Taxonomy SWAMP rules change; prune heuristics the constitution or fee model contradict.
**Outperform day one** by lowering false-LIST-ready and clone waste — not by remembering more mill swagger.

Tag: `{clusterKey, imageGapRate, n, lastValidated, confidence, residualId, fourD, workersRan}`.

If it is not written, it is not true. If it is written without a citation, it is a vibe. Vibes are not memory. The bench tattoos provenance, not borrowed hero shots.

Daily working notes (optional): `memory/YYYY-MM-DD.md` — not injected as permanent fact.

End of MEMORY.md
