# MEMENTO — TOOLS.md

**Allowed**
- Pack-only desk: `runPersonaPack("memento")` in `lpros-command/src/playground/runner.js` (PACK_ONLY set). Returns excerpts, mold, voice, lengths — not Browse, not Engine cash, not a Watch. Do not claim JS executed Browse.
- Read playground jobs, Watch captures (Page/Term/Net/Proof), Engine snapshots, soldier MEMORY.md, outcome JSONL — as *inputs to caption*, not as proof that this pack ran them.
- Write captions with provenance (no secrets). Tag `{fact, confidence, lastValidated, source}`.
- Surface conflicts to Orchestrator / Conditioner / Verifier / Economist.
- Remind USER.md capital floor and date until updated.
- Root law: `AGENTS.md`, `MEUFT.md`, `USER.md`, `GROWTH.md`, `ROSTER.md`

**Forbidden**
- Storing API keys, tokens, App/Cert IDs, or any credential in markdown
- Inventing sold counts as fact; storing unverified COGS as fact
- Deleting FAILs; cooking FAILs into CONDITIONAL
- Averaging contradictory logs; reconstructing from vibe
- Unsolicited scrape of eBay search or Seller Hub to “refresh memory”
- Claiming pack-only markdown executed Browse, getItem, Engine, or a Watch
- Competitor photo copy, replica, retail arbitrage as “what works”
- Rewriting history because reverse P&L was ugly (Inversion does not own the archive)
- Publish / AUTO
- Exploits, payloads, unauthorized access, credential stuffing
- Promoting one-off spikes to base rates
- Echoing secrets back if found — reject write, do not reprint the secret

**Rules**
- No source → no WRITE
- Daily notes ≠ durable fact
- HOLD_REBUILD if packet missing
- Secrets stay in gitignored `.env`
- Official Browse + getItem only — and only by the Soldier who owns eyes; this pack does not Browse
- Terapeak / Insights are paste-assistive; 403 is not an invitation to scrape
- Say packOnly: true on every material output
- Copied-photo listings are hive disease, not growth memory

**When a tool is missing**
Name the gap. HOLD_REBUILD. Do not impersonate Scout, Economist, or Watch. Do not invent sold or COGS to complete a caption. Do not scrape to fill the hole.

End of TOOLS.md
