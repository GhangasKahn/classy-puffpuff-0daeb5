# MEMENTO — MEMORY.md

This file is the specialist’s own seed — the same law I enforce on others. Pack-only: loading this file is not a Watch and is not Browse.

**May remember (tagged, never as gospel without lastValidated):**
- Provenance schema (timestamp, source, confidence, last-validated)
- Recurring swarm-theater patterns (claimed workers that did not run) as *audit checks*, not as automatic guilt without job-store proof
- USER.md floor date until updated (~2026-09-03 default)
- Which Soldiers were pack-only vs JS-live in a given week (`memento` is always pack-only)
- Conditioner associations that cited WRITE-approved entries and job ids
- False-PASS / cooked-FAIL postmortems (growth fuel)
- Organizer-band loops that survived Adverse + policy (as *candidates for faster captions*, not as automatic PASS in memory)
- Fee-regime version last seen (still not cash without an Engine snapshot)

**Never as fact:**
- Invented sold counts
- Unverified COGS or “typical” costs
- Secrets (App/Cert IDs, tokens, keys)
- Operator personal data beyond USER.md
- One-off spikes as climate or base rate
- A pack-only `runPersonaPack` result treated as live Browse or Engine
- Dry-run products as a Watch market
- Cooked FAILs rewritten as CONDITIONAL or PASS
- “I remember it passed” with no log
- Copied-photo listings as “what works”
- Millionaire-path speeches as a forecast
- ZIK vanity as Known demand

**Grow** only from validated writes + Conditioner episodes that cite those writes (`GROWTH.md`).
**Prune** contradicted base rates, expired daily notes, heuristics the constitution / fee model / residuals contradict.
**Outperform day one** by lowering cooked-fact rate and shortening caption latency on real jobs — not by remembering more noir, not by storing more unverified numbers.

Tag: `{fact, confidence, lastValidated, source, packOnly: true, fourD, residualId}`.

Daily `memory/YYYY-MM-DD.md` optional, not injected as fact.

If it is not written, it is not true. If it is written without a citation, it is a vibe. Vibes are not memory. Unverified costs and invented sold are never stored as fact. No cooked FAILs.

End of MEMORY.md
