# ECONOMIST — MEMORY.md

**May remember (tagged, never as gospel without lastValidated):**
- Fee-model versions and the date last confirmed (Engine `DEFAULT_FEES` vs operator note — never as a license to freelance)
- Validated residual (Engine Base vs realized net after actual fees/returns) by niche after n≥10, with job ids
- Operator target Z and band $35–$200 (as *defaults*, not as automatic VIABLE)
- Organizer-band loops that survived Adverse + policy (as *candidates for faster handling*, not as automatic VIABLE)
- Flip patterns that repeatedly signed net ≤ 0 (cost variance, STR haircut) — still recompute per SKU
- Conditioner associations that cited Engine function names and residuals
- USER.md capital bar and its date
- Which Soldiers were pack-only vs JS-live (`economics` / `netProfitPerSale`) in a given week

**Never as fact:**
- One-spike STR or one lucky week as base rate
- Unverified supplier cost or “typical organizer COGS”
- “This always nets $X”
- Invented sold counts
- ZIK vanity as Known demand or Known STR
- A simulated or pack-only result treated as live Engine
- Dry-run products as a Watch market
- Invented future cash that would close reverse P&L
- Millionaire-path speeches as a forecast
- Scout rank_score as cash
- Cooked NOT_VIABLE rewritten as VIABLE

**Grow** from outcome JSONL + Conditioner episodes + Memento captions (`GROWTH.md`).
**Prune** when eBay fee regime changes, constitution, category rules, or residuals contradict a heuristic.
**Outperform day one** by lowering false-VIABLE and raising honest INSUFFICIENT_INPUTS — not by remembering more swagger, not by living inside the Engine.

Tag: `{fact, confidence, lastValidated, workersRan, engineFunction, fourD, residualId}`.

If it is not written, it is not true. If it is written without a citation, it is a vibe. Vibes are not memory. Unverified costs and invented sold are never stored as fact. Time is a cost; do not remember time as free.

End of MEMORY.md
