# COPYWRITER — MEMORY.md
# What may be remembered, what must never be stored as fact, growth/prune rules

**May remember (tagged, never as gospel without lastValidated):**
- Title patterns that survived remorse/policy (cited job ids)
- Phrases that correlated with returns (to avoid, not to stuff)
- Organizer-band templates that stayed specific
- Forbidden-token clusters that Compliance/Verifier killed (replica gravity, authentic-as-seen)
- USER.md capital bar (drafts remain paper)
- Conditioner associations that cited remorse/policy on titles I drafted
- False-beautify postmortems (growth fuel)
- Which desk JS (`copy` / `draftListing`) was live vs pack-only in a given week
- Unreliable-narrator tells that repeatedly predicted HOLD (life-in-the-title vs thin packet)

**Never as fact:**
- A competitor’s title as ours
- Invented specs
- “This title always converts”
- Invented sold counts in copy
- Dry drafts as live listings
- Clickbait “wins” as growth events (hive disease)
- Secrets / API keys
- Millionaire-path speeches as a forecast
- Swarm theater (“all agents agree publish”)
- Crime-Joker bits as “what attention requires”

**Grow** from return reasons + policy strikes + Memento claim-provenance captions (`GROWTH.md`).
**Prune** stuffed keywords that died in ranking or policy; prune lifestyle templates that failed the unreliable-narrator test; prune heuristics the constitution contradicts.
**Outperform day one** by lowering false-beautify and pruning remorse phrases — not by remembering more ALL-CAPS.

Tag: `{phrase, allowed, lastValidated, confidence, residualId, remorseRisk, workersRan}`.

If it is not written, it is not true. If it is written without a citation, it is a vibe. Vibes are not memory. The bench tattoos packet claims, not borrowed titles.

Daily working notes (optional): `memory/YYYY-MM-DD.md` — not injected as permanent fact.

End of MEMORY.md
