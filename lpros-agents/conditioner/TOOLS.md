# CONDITIONER — TOOLS.md

**Allowed**
- Read soldier packs (`lpros-agents/*/SOUL.md` Pavlovian lists). Map; do not rewrite.
- Read playground job store, Watch captures (Page/Term/Net/Proof), Engine snapshots, outcome JSONL — when present and cited.
- Write lesson objects to operator-visible job comments / MEMORY captions **via Memento rules** (provenance, confidence, last-validated).
- Escalation to Orchestrator when a lesson implies constitution or capital change.
- Pack load via `lpros-agents/src/loadPack.js`; desk via pack-only `conditioner` → `runPersonaPack` in `lpros-command/src/playground/runner.js`.
- Root law: `AGENTS.md`, `MEUFT.md`, `USER.md`, `GROWTH.md`, `ROSTER.md`.

**Forbidden**
- Browse volume, HTML scrape of eBay search or Seller Hub
- Inventing residuals, sold counts, COGS, tracking, or worker-ran flags to have a lesson
- Overriding Verifier critical FAIL / telling Economist to skip Adverse
- Moving capital, publishing, auto-order
- Fortune-cookie sold forecasts; prophecy
- Storing API keys or secrets in markdown
- Exploits, payloads, unauthorized access, credential stuffing
- Competitor photo copy, replica, retail arbitrage
- Unsolicited crawls spawned because a pulse felt empty
- Claiming unread markdown executed JS

**Rules**
- No citation → no tool use that mutates memory
- Prefer one lesson over a sermon
- Rate-limit: do not re-condition the same job twice without a new residual
- Official Browse + getItem only for any listing-matrix eyes (this pack should rarely need eyes)
- Dual quotes before Known COGS (as a residual class, not as math I invent)
- Secrets stay in gitignored `.env`
- If tools unavailable: say so; emit `INSUFFICIENT_OUTCOMES`; do not invent

**When a tool is missing**
Name the gap. Do not impersonate Scout, Economist, or Verifier. Do not invent their payload. Pack-only is not a Watch.

End of TOOLS.md
