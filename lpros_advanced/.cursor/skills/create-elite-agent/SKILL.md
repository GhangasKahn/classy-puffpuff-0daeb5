---
name: create-elite-agent
description: Spawn a new LPROS elite Hermes soldier. Canonical skill: lpros-agents/.cursor/skills/create-elite-agent/SKILL.md. Live roster includes orchestrator (ruler), scout, intel, verifier, economist, fulfiller, copywriter, conditioner, redteam, pressure, compliance, taxonomy, memento, inversion, factory, wick, browser, vm, swarm.
---

# Create Elite Agent

Canonical copy lives at `lpros-agents/.cursor/skills/create-elite-agent/SKILL.md`. Follow that file.

## Law

Root `lpros-agents/AGENTS.md` and `GROWTH.md` are binding. Soldier `AGENTS.md` = procedures only. Quality bar is `orchestrator/` (ruler).

## Atomic sequence

1. Read root constitution + `GROWTH.md` + `orchestrator/` as the bar.
2. Write `SOUL.md`, `IDENTITY.md`, `AGENTS.md`, `TOOLS.md`, `HEARTBEAT.md`, `MEMORY.md` under `lpros-agents/<id>/`.
3. Wire IDENTITY to desk JS if it exists (`Desk JS` line + JSON contract). HEARTBEAT must forbid unsolicited scrapes.
4. Update `lpros-agents/README.md`, `ROSTER.md`, `INSPIRATION.md`, `src/loadPack.js`, playground catalog.
5. Never put secrets in markdown. Never invent sold counts. Never scrape eBay search HTML. Never write crime how-tos.
